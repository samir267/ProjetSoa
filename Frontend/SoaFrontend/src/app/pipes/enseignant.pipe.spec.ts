import { EnseignantPipe } from './enseignant.pipe';

describe('EnseignantPipe', () => {
  it('create an instance', () => {
    const pipe = new EnseignantPipe();
    expect(pipe).toBeTruthy();
  });
});
